// Deprecated — superseded by plugins/reveal.ts (universal registration).
// Kept as a no-op because this environment couldn't delete the file.
// Safe to delete from your repo.
export default defineNuxtPlugin(() => {})
